"This file is used as an argument for debugging"

(extern i32 printf ptr vararg)

(fun i32 main () (
	(if (== 3 2) (
		(printf "3 == 2 (for some reason)\n")
	))
	(elif (> 3 2) (
		(printf "3 > 2\n")
	))
	(elif (== 1 1) (
		(printf "1 == 1\n")
	))
	(else (
		(printf "None of them are correct\n")
	))
	(endif)

	(return 0)
))
